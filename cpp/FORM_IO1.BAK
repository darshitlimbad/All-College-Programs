#include <iostream.h>
#include<conio.h>

int main() {
	clrscr();
    int a=10;
    float x=13.34332;

    cout.width(10);
    cout<<a<<endl;

    cout.precision(3);
    cout<<x<<endl;

    cout.width(10);
    cout.fill('*');
    cout << a << endl;

    cout.width(10);
    cout.fill('*');
    cout.setf(ios::internal);
    cout.setf(ios::showpos);
    cout << a << endl;

    getch();
    return 0;
}