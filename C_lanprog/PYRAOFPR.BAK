void main()
{
	int i,j,temp , n , space ,k=0  ;

	int prime_num[1000],*store , stop;

	clrscr();
	store=prime_num;
	printf("enter your desired val for LIMITE:");
	scanf("%d",&n);

	for(i=1 ; i<=n ; i++)
	{
		  temp=0;
		  for(j=2 ; j<=i/j ; j++)
		  {
			if(i%j==0)
			{	temp=1;
				break;
			}
		  }
		  if(temp==0)
		  {
			printf("\t%d\t",i);
			*store++=i;
			stop=i;
			//store++;
		  }
	}

	for(temp=1 ; temp<=40 ; temp++)
		printf("-");
	printf("\n");

	for(i=1 ; i<=stop ; i++)
	{
		for(space=stop ; space>=i ; space--)
		{
			printf("   ");
		}
		for(j=1 ; j<=i ; j++)
		{	printf(" %d   ",prime_num[k]);
			k++;

		}
		printf("\n");
	}

	getch();
}
//@darshit_limbad